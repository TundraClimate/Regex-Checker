# Regex Checker
check if regex matches
## How to Use
```shell
#run *.jar
java -jar [filename].jar
```

#### Step. 1
typing a **Regex**
```
Please type a Regex> [regex]
```
`[example regex]:`
```regexp
^-?[1-9][0-9]+$|^-?[1-9]$|^0$
```